# Node / package managers
node_modules/
.pnpm-store/
.pnp/
.pnp.js
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.wrangler/
# Build outputs
dist/
dist-wrangler/
dist-*/
build/
coverage/

# Optional local client checkouts. Yurumeet/mobile clients live in separate repos
# and consume @takosjp/yurucommu-api instead of being committed here.
clients/

# Tooling caches
.vite/
.cache/
.eslintcache
*.tsbuildinfo

# Env files (keep examples)
.env
.env.*
!.env.example

# Logs
logs/
*.log

# Local runtime / operator state — never commit (self-host server scratch dir:
# SQLite DBs, uploaded media, captured logs, cookie jars, credentials).
data/

# Operator deploy config — realized resource IDs / routes live outside the repo
# (see AGENTS.md: operator config + secrets are kept out of the repo).
wrangler.toml
wrangler.local.toml

# Temp files
tmp/
temp/
*.tmp

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~
