{
  "name": "@takosjp/yurucommu-core",
  "version": "2.0.0",
  "license": "GPL-3.0-only",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "exports": {
    ".": "./src/backend/public.ts",
    "./server": "./src/backend/public.ts",
    "./client-api": "./packages/api/src/index.ts",
    "./plugins": "./src/plugin/public.ts"
  },
  "imports": {
    "#test/assert": "./shims/std/assert.ts",
    "#test/mock": "./shims/std/mock.ts",
    "#test/time": "./shims/std/time.ts"
  },
  "scripts": {
    "start": "bun src/backend/server.ts",
    "dev": "bunx wrangler dev --config wrangler.local.toml",
    "dev:server": "bun src/backend/server.ts",
    "check": "tsc --noEmit",
    "test": "bun run build:api && bun test test/ src/backend/ packages/api/src/",
    "test:backend": "bun test src/backend/",
    "build": "bun run build:api && bun run build:capsule-worker",
    "build:api": "cd packages/api && bun run build",
    "pack:api": "cd packages/api && bun run pack:dry",
    "build:worker": "bun x esbuild src/backend/index.ts --bundle --format=esm --outfile=dist/worker.js --platform=browser --target=es2022 --conditions=workerd,worker,browser --external:cloudflare:* --external:node:*",
    "build:capsule-worker": "bun scripts/build-takos-worker.ts",
    "build:takos-worker": "bun scripts/build-takos-worker.ts",
    "app:activate": "bun scripts/apply-takosumi-migrations.ts",
    "takosumi:release": "bun scripts/takosumi-release.ts",
    "deploy": "bun run build && bunx wrangler deploy --config wrangler.toml",
    "lint": "bun run check",
    "fmt": "bunx prettier --write src test packages/api",
    "fmt:check": "bunx prettier --check src test packages/api",
    "db:generate": "bunx drizzle-kit generate",
    "db:push": "bunx drizzle-kit push",
    "db:migrate": "bunx drizzle-kit migrate",
    "db:studio": "bunx drizzle-kit studio"
  },
  "dependencies": {
    "@takosjp/yurucommu-api": "workspace:*",
    "@aws-sdk/client-s3": "^3.971.0",
    "@libsql/client": "^0.17.0",
    "better-sqlite3": "^12.4.1",
    "drizzle-orm": "^0.45.1",
    "fdir": "^6.5.0",
    "hono": "^4.12.9",
    "picomatch": "^4.0.4",
    "tinyglobby": "^0.2.16"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260420.1",
    "@types/better-sqlite3": "^7.6.11",
    "@types/bun": "latest",
    "@types/node": "^22.0.0",
    "drizzle-kit": "^0.31.9",
    "esbuild": "^0.25.12",
    "typescript": "^5.7.0",
    "wrangler": "^4.103.0"
  }
}
