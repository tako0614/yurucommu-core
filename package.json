{
  "name": "takos",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "backend",
    "platform"
  ],
  "scripts": {
    "dev": "npm --workspace backend run dev",
    "deploy": "npm --workspace backend run deploy",
    "lint": "npm --workspaces run lint || true",
    "test": "npm --workspace backend run test && npm --workspace platform run test"
  },
  "dependencies": {
    "ky": "^1.14.0"
  }
}
