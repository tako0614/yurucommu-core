apiVersion: v1

metadata:
  id: com.yurucommu.app.build
  name: Yurucommu build

nodes:
  web:
    kind: linux-container
    spec:
      image: oven/bun:1.3.14-alpine
      command: bun install --frozen-lockfile && bun run build:worker
    dependsOn: []
