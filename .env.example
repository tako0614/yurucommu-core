# =============================================================================
# Yurucommu - Environment Variables
# =============================================================================
# This file documents all environment variables used by yurucommu.
# Copy this file to .dev.vars for local development with wrangler.
#
# For Cloudflare Workers deployment:
# - Set via wrangler.toml [vars] for non-sensitive config
# - Set via `wrangler secret put <NAME>` for secrets
# =============================================================================

# -----------------------------------------------------------------------------
# Application Configuration (wrangler.toml [vars])
# -----------------------------------------------------------------------------
APP_URL=https://app.yurucommu.com

# Takos Platform URL (for OAuth and API integration)
TAKOS_URL=https://takos.jp

# -----------------------------------------------------------------------------
# Authentication - Password (Optional)
# Set via: wrangler secret put AUTH_PASSWORD
# Simple password authentication for single-user instances
# -----------------------------------------------------------------------------
AUTH_PASSWORD=your-secure-password

# -----------------------------------------------------------------------------
# Authentication - Google OAuth (Optional)
# Set via: wrangler secret put GOOGLE_CLIENT_ID
#          wrangler secret put GOOGLE_CLIENT_SECRET
# -----------------------------------------------------------------------------
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# -----------------------------------------------------------------------------
# Authentication - X/Twitter OAuth (Optional)
# Set via: wrangler secret put X_CLIENT_ID
#          wrangler secret put X_CLIENT_SECRET
# -----------------------------------------------------------------------------
X_CLIENT_ID=your-x-client-id
X_CLIENT_SECRET=your-x-client-secret

# -----------------------------------------------------------------------------
# Authentication - Takos OAuth (Optional)
# Set via: wrangler secret put TAKOS_CLIENT_ID
#          wrangler secret put TAKOS_CLIENT_SECRET
# Enables integration with Takos platform
# -----------------------------------------------------------------------------
TAKOS_CLIENT_ID=your-takos-client-id
TAKOS_CLIENT_SECRET=your-takos-client-secret

# -----------------------------------------------------------------------------
# Session Management (Required)
# Set via: wrangler secret put SESSION_SECRET
# Generate with: openssl rand -base64 32
# -----------------------------------------------------------------------------
SESSION_SECRET=your-64-character-hex-session-secret

# =============================================================================
# Database Configuration
# =============================================================================
# For local development with SQLite/LibSQL:
DATABASE_URL=file:./dev.db

# For production with Cloudflare D1 (configured in wrangler.toml):
# D1 binding is configured via [[d1_databases]] in wrangler.toml

# =============================================================================
# Cloudflare Bindings (Configured in wrangler.toml, not here)
# =============================================================================
# The following are configured as bindings in wrangler.toml:
#
# D1 Database:
#   binding = "DB"
#   database_name = "yurucommu-db"
#
# R2 Bucket (for media storage):
#   binding = "MEDIA"
#   bucket_name = "yurucommu-media"
#
# KV Namespace (for sessions, OAuth state):
#   binding = "KV"
#   id = "<your-kv-namespace-id>"  # Create with: wrangler kv:namespace create
#
# Static Assets:
#   binding = "ASSETS"
# =============================================================================
